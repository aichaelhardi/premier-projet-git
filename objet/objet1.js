const personne ={
    nom : "elhardi",
    prenom : "aicha",
    age : 22,
};
console.log("informations de personne:" );
console.log(`nom : ${personne.nom}` );
console.log(`prenom : ${personne . prenom}` );
console.log(`age : ${personne. age} ans` );